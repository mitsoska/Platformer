#ifndef _CONFIG_H
#define _CONFIG_H

#include <SDL2/SDL_ttf.h>
#include <SDL2/SDL_image.h>

struct Config
{
  TTF_Font *font_small, *font_medium, *font_big, *font_bigger;

  void initialize();

  SDL_Texture* button_normal;
  SDL_Texture* button_hover;
};


extern Config config;


#endif
